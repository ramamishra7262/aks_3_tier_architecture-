# IMPORTANT: In production use Azure Key Vault + External Secrets Operator
# Never commit real secrets to Git — these are placeholder values only
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: app
  annotations:
    # If using External Secrets Operator:
    # externalsecrets.io/vault-path: "secret/app"
type: Opaque
stringData:
  DB_PASSWORD: "CHANGE_ME_USE_KEY_VAULT"
  JWT_SECRET:  "CHANGE_ME_USE_KEY_VAULT"
  REDIS_PASSWORD: ""
